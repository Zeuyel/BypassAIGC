# ============================================
# AI 学术写作助手 - Docker 部署配置
# ============================================
# 
# 安全提示：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 修改所有密码和密钥
# 3. 不要将 .env 文件提交到版本控制
#
# ============================================

# --------------------------------------------
# 应用配置
# --------------------------------------------
APP_PORT=23110

# CORS 配置（允许的源，多个用逗号分隔）
ALLOWED_ORIGINS=https://polish.zufe.top,http://localhost:23110,http://127.0.0.1:23110

# --------------------------------------------
# 数据库配置（PostgreSQL）
# --------------------------------------------
DB_USER=aipolish
DB_PASSWORD=请修改为强密码_至少16位_包含大小写字母数字特殊字符

# --------------------------------------------
# Redis 配置
# --------------------------------------------
REDIS_PASSWORD=请修改为强密码_至少16位

# --------------------------------------------
# 安全配置（必须修改）
# --------------------------------------------
# JWT 密钥（生成方法：python -c "import secrets; print(secrets.token_urlsafe(32))"）
SECRET_KEY=请修改为随机字符串_至少32位

# 管理员账户
ADMIN_USERNAME=admin
ADMIN_PASSWORD=请修改为强密码_至少12位

# --------------------------------------------
# AI 模型配置 - 第一阶段（论文润色）
# --------------------------------------------
POLISH_MODEL=gpt-4
POLISH_API_KEY=your-api-key-here
POLISH_BASE_URL=https://api.openai.com/v1

# --------------------------------------------
# AI 模型配置 - 第二阶段（原创性增强）
# --------------------------------------------
ENHANCE_MODEL=gpt-4
ENHANCE_API_KEY=your-api-key-here
ENHANCE_BASE_URL=https://api.openai.com/v1

# --------------------------------------------
# AI 模型配置 - 感情文章润色（可选）
# --------------------------------------------
EMOTION_MODEL=gpt-4
EMOTION_API_KEY=your-api-key-here
EMOTION_BASE_URL=https://api.openai.com/v1

# --------------------------------------------
# 并发配置
# --------------------------------------------
MAX_CONCURRENT_USERS=5
DEFAULT_USAGE_LIMIT=1
SEGMENT_SKIP_THRESHOLD=15

# --------------------------------------------
# 会话压缩配置
# --------------------------------------------
HISTORY_COMPRESSION_THRESHOLD=2000
COMPRESSION_MODEL=gpt-4
COMPRESSION_API_KEY=your-api-key-here
COMPRESSION_BASE_URL=https://api.openai.com/v1

# --------------------------------------------
# 文件上传配置（MB，0 表示无限制）
# --------------------------------------------
MAX_UPLOAD_FILE_SIZE_MB=10

# --------------------------------------------
# JWT 配置
# --------------------------------------------
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

